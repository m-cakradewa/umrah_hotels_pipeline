name: umrah_hotels
version: 1.0
profile: umrah_hotels

model-paths: ["models"]
models:
  umrah_hotels:
    +materialized: table
    +pre-hook:
      - "create schema if not exists {{ this.schema }};"
    silver:
      +schema: silver
      +database: null
    gold:
      +schema: gold